var iconService = new IconService(new HttpProvider('http://127.0.0.1:9000/api/v3:9000'));

/*
App = {
	iconProvider: null,

	init: function() {
 return App.initiconService();
},
*/

//initiconService: function() {
//	const iconService = new IconService(new HttpProvider('http://127.0.0.1:9000/api/v3:9000'));

//},

/*

setPersonName: function() {
	const transaction = transactionBuilder
	.nid(networkId)
	.from(wallet.getAddress())
	.to(scoreAddress)
	.value(new BigNumber("150000000"))
	.stepLimit(new BigNumber("1000000"))
	.nonce(new BigNumber("1000000"))
	.build();
	console.log(transaction);
},

getPersonName: function() {
	const transaction = transactionBuilder
	.nid(networkId)
	.from(wallet.getAddress())
	.to(scoreAddress)
	.value(new BigNumber("150000000"))
	.stepLimit(new BigNumber("1000000"))
	.nonce(new BigNumber("1000000"))
	.call("transfer")
	.params(params)
	.build();
	console.log(transaction);
}
}





$(function() {
$(window).load(function() {
	App.init();
	
});
});
*/